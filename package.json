{
  "name": "portal-platform",
  "version": "1.0.0",
  "description": "Portal Platform with Angular frontend and Node.js backend",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && ng serve",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && ng build --prod"
  },
  "keywords": ["angular", "nodejs", "express", "portal"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
